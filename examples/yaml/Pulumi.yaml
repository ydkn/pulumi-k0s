name: pulumi-k0s
runtime: yaml

resources:
  myClusterResource:
    type: pulumi-k0s:Cluster
    properties:
      spec:
        hosts:
          - role: "controller+worker"
            hostname: "my-node"
            localhost:
              enabled: true

outputs:
  output:
    value: ${myClusterResource.kubeconfig}
