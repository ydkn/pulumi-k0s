# WARNING: This file is autogenerated - changes will be overwritten if not made via https://github.com/pulumi/ci-mgmt

project_name: pulumi-k0s
builds:
- id: build-provider
  dir: provider
  env:
  - CGO_ENABLED=0
  - GO111MODULE=on
  goos:
  - darwin
  - linux
  goarch:
  - amd64
  - arm64
  ignore: &a1 []
  main: ./cmd/pulumi-resource-k0s/
  ldflags: &a2
    - -s
    - -w
    - -X
      github.com/pulumi/pulumi-k0s/provider/pkg/version.Version={{.Tag}}
  binary: pulumi-resource-k0s
- id: build-provider-sign-windows
  dir: provider
  env:
  - CGO_ENABLED=0
  - GO111MODULE=on
  goos:
  - windows
  goarch:
  - amd64
  - arm64
  ignore: *a1
  main: ./cmd/pulumi-resource-k0s/
  ldflags: *a2
  binary: pulumi-resource-k0s
  hooks:
    post:
    - make sign-goreleaser-exe-{{ .Arch }}
archives:
- name_template: "{{ .Binary }}-{{ .Tag }}-{{ .Os }}-{{ .Arch }}"
  id: archive
snapshot:
  version_template: "{{ .Tag }}-SNAPSHOT"
changelog:
  disable: true
release:
  disable: false
blobs:
- provider: s3
  region: us-west-2
  bucket: get.pulumi.com
  ids:
  - archive
